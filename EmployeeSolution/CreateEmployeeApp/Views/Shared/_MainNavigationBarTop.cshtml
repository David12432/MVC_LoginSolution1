<div class="nav">
    <ul class="nav">
        <li>@Html.ActionLink("Home", "Index", "Home")</li>
        @if (Session["userInfo"] == null)
        {
        <li>@Html.ActionLink("Login", "Login", "CreateAccount")</li>
        <li>@Html.ActionLink("Create Account", "CreateAccount", "CreateAccount")</li>
        }
        @if (Session["userInfo"] != null)
        {
        <li>@Html.ActionLink("Logout", "Logout", "CreateAccount")</li>
        }
    </ul>
</div>


